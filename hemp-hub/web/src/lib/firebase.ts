import { api } from './api-adapter';

export const firebase = api;

export const firebaseConfig = {
  // Dummy config to satisfy imports
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};